import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  /* config options here */
  // TODO: Add environment-specific configurations for different deployment targets
  // TODO: Configure API proxy settings for development vs production
}

export default nextConfig
